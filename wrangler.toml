name = "mungnyang-clock"
compatibility_date = "2026-09-01"
workers_dev = true

# 우리 도메인 (2026-09-10 등록). 표(assets)보다 먼저 와야 최상위 설정으로 읽힌다.
routes = [
  { pattern = "pawminute.com", custom_domain = true },
  { pattern = "www.pawminute.com", custom_domain = true },
]

[assets]
directory = "./public"
not_found_handling = "404-page"
